extends layout

block content
  h1 Welcome to #[span.heading The Board] #[a(href='/new', title='Add new message').post  &plus;]
  main
    each post in posts
      article 
        div.message= post.message
        hr
        div.author= post.author
        div.added= '[ ' + post.added + ' ]'